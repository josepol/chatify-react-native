import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
		flex: 1,
        backgroundColor: 'white',
        paddingTop: 20
    },
    input: {
        borderColor: 'black',
        borderWidth: 2
    }
});